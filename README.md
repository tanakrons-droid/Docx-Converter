# Docx Converter - แปลง Google Docs เป็น WordPress Gutenberg

เครื่องมือแปลง HTML จาก Google Docs เป็น WordPress Gutenberg Blocks อัตโนมัติ พร้อมรองรับการจัดรูปแบบและโครงสร้างที่ซับซ้อน

## ✨ ฟีเจอร์หลัก

### 📝 การแปลง Blocks
- **Heading (H1-H6)** - แปลงหัวข้อพร้อม anchor ID สำหรับ jump link
- **Paragraph** - รองรับการจัดตำแหน่ง (ซ้าย/กลาง/ขวา) และสี
- **List (ul/ol)** - รองรับ nested list หลายระดับ
- **Table** - แปลงตารางพร้อม thead/tbody, colspan, rowspan
- **Image** - รองรับ caption และ alt text
- **Separator** - เส้นคั่นอัตโนมัติ
- **YouTube Embed** - แปลง YouTube links เป็น embed block
- **Button** - แปลงปุ่มลิงก์

### 🎨 การจัดรูปแบบ
- **Bold/Italic/Underline** - รักษาการจัดรูปแบบข้อความ
- **Color** - ดึงสีข้อความจาก style (ไม่รวมสีดำ)
- **Alignment** - ตรวจจับการจัดตำแหน่งจาก CSS
- **Links** - รักษา hyperlinks พร้อม target="_blank" สำหรับ external links

### 📋 ฟีเจอร์พิเศษ
- **สารบัญ (TOC)** - แปลง list แรกเป็น listmenu พร้อม anchor links
- **Caption** - ตรวจจับ caption ใต้รูปภาพ (ตัวเอียง/จัดกลาง)
- **Headline Detection** - ตรวจจับหัวข้อพิเศษ เช่น "บทความแนะนำ", "โปรแนะนำ"
- **Q&A Section** - รองรับส่วน Q&A พร้อม class พิเศษ
- **References** - รองรับส่วนอ้างอิง

## 🚀 วิธีใช้งาน

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. รันโปรเจค
```bash
npm run dev
```

### 3. เปิดเบราว์เซอร์
เข้า `http://localhost:5173`

### 4. วิธีแปลงเอกสาร
1. เปิด Google Docs
2. ไปที่ **File > Download > Web Page (.html, zipped)**
3. แตกไฟล์ zip
4. เปิดไฟล์ .html ในเบราว์เซอร์
5. **Ctrl+A** เลือกทั้งหมด แล้ว **Ctrl+C** คัดลอก
6. วางใน Input ของเว็บ
7. เลือก Website (เช่น vsquare, rattinan)
8. กด **Convert**
9. คัดลอก Output ไปวางใน WordPress

## 📁 โครงสร้างโปรเจค

```
docxconverter-main/
├── src/
│   ├── components/
│   │   ├── Home.jsx          # หน้าหลัก
│   │   └── HtmlToGutenberg.jsx
│   ├── utils/
│   │   └── htmlToGutenbergConverter.js  # Logic หลักในการแปลง
│   └── App.jsx
├── html-to-gutenberg/        # Library แยก
└── package.json
```

## 🔧 ฟังก์ชันหลักใน htmlToGutenbergConverter.js

### `convert(inputHtml, options)`
ฟังก์ชันหลักสำหรับแปลง HTML เป็น Gutenberg

**Parameters:**
- `inputHtml` - HTML string จาก Google Docs
- `options` - ตัวเลือกเพิ่มเติม
  - `website` - เว็บไซต์ปลายทาง (เช่น 'vsquare', 'rattinan')

**Returns:**
```javascript
{
  html: "<!-- wp:heading -->...",  // Gutenberg HTML
  report: {
    inputLength: 1000,
    outputLength: 800,
    blocksCreated: 25,
    executionTimeMs: 50
  }
}
```

### `cleanHTML(html)`
ทำความสะอาด HTML - ลบ styles ที่ไม่จำเป็น, merge nested lists

### `applyPolicies(html, config)`
ใช้ policies ตรวจสอบและปรับปรุง HTML
- ลบ tags ต้องห้าม
- ตรวจสอบ headings
- เพิ่ม disclaimer

## ⚙️ การตั้งค่า Website

แต่ละ website มี footer และ settings ที่แตกต่างกัน:

```javascript
const websiteConfig = {
  vsquare: {
    footer: '<!-- wp:block {"ref":66914} /-->',
    domain: 'vsquareclinic.com'
  },
  rattinan: {
    footer: '<!-- wp:block {"ref":12345} /-->',
    domain: 'rattinanclinic.com'
  }
};
```

## 🐛 การแก้ปัญหาที่พบบ่อย

### ตารางแสดงไม่ถูกต้อง
- ตรวจสอบว่าตารางใน Google Docs มีโครงสร้างที่ถูกต้อง
- colspan/rowspan จะถูกลบถ้าค่าเป็น "1"

### รูปภาพไม่แสดง
- ต้อง upload รูปไปยัง WordPress แยก
- แทนที่ URL ใน output ด้วย URL จริง

### Anchor links ไม่ทำงาน
- ตรวจสอบว่า heading มี ID ที่ตรงกับ href ในสารบัญ

## 📝 License

MIT License

## 👨‍💻 ผู้พัฒนา

Tanakron's Droid
